# Deploy Queue App
Simple NextJS app using WebSockets to create a queue of pull requests that are due to be deployed, so that a large team working on a single repo can keep in sync and not constantly be stuck with merge conflicts by deploying too close to one another.